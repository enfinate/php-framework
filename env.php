<?php
    //Global Variable file
    $env = array(
        // Base route is a route where it will automatically call once website load 
        "base_route"=>"home",
        "mysql"=>array(
            "username"=>"root",
            "password"=>"",
            "database"=>"softphp",
            "localhost"=>"localhost",
        )
    );